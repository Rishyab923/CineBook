apiVersion: v1
kind: Service

metadata:
  name: cinebook-frontend
  namespace: cinebook

spec:
  type: NodePort

  selector:
    app: cinebook-frontend

  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
